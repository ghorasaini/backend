function auth (req, res, next){
    const token = req.headers["authorization"];
    const realToken = token.split (" ")[1];
    console.log (realToken)

    console.log (token)
     if (realTokentoken === "123"){
        next()

     }else {
        res.send ("Forbidden: Invalid token")
     }
}
export {auth};